---

ansible_distribution: "{{ lookup('env','ANSIBLE_DISTRIBUTION') }}"

debian_deps:
  - openssh-server
  - sudo
  - wget
  - curl
  - htop
  - vim
  - tmux
  - iputils-ping
  - nmap
  - python3-pip

centos_deps:
  - openssh-server
  - openssh-clients
#  - openssh
  - iputils
  - nmap
  - libss
  - sudo
  - wget
  - curl
  - vim
  - tmux
